# Быстрый старт 🚀

## Локальная разработка

### 1. Backend

```bash
cd backend
npm install
npm run start:dev
```

Backend запустится на http://localhost:3000

### 2. Admin

```bash
cd admin
npm install
npm run dev
```

Админка откроется на http://localhost:3001

### 3. Mobile

```bash
cd mobile
npm install
# Для iOS
npm run ios
# Для Android  
npm run android
```

**Важно**: Перед запуском на устройстве измените `API_URL` в `mobile/src/services/api.ts` на IP-адрес вашего компьютера в локальной сети (например, `http://192.168.1.100:3000`)

## Первый запуск

1. Запустите backend
2. Откройте админку и добавьте сообщения для точек (опционально)
3. Запустите мобильное приложение
4. Игра готова!

## Особенности

- ✅ Все ходы просчитаны заранее - финиш гарантирован 2 февраля
- ✅ Один ход в день (по местному времени устройства)
- ✅ Сообщения можно редактировать в админке в любой момент
- ✅ Карта скроллится только по посещенным точкам
- ✅ Красивые анимации и пиксель-арт стиль

## Структура

- `backend/` - NestJS API с SQLite базой
- `admin/` - Веб-админка на React + Vite
- `mobile/` - React Native приложение с MobX

Удачи! 🎁

